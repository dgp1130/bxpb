{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "prepack": "npm run -- lerna bootstrap --ci && npm run -- lerna run clean"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
